import PageHeader from '@/components/shell/page-header'; import Panel from '@/components/ui/panel'; import Badge from '@/components/ui/badge'; import { requireAdmin } from '@/lib/auth'; import { getForecastRuns } from '@/lib/scm'; import { runBaseline } from './actions';
export const dynamic = 'force-dynamic';
export default async function ForecastRunsPage() { await requireAdmin(); const { rows, error } = await getForecastRuns(); return <div className="page-content"><PageHeader title="Forecast Runs" description="Forecast 실행 이력과 데이터 snapshot 상태를 확인합니다." /><Panel><form action={runBaseline}><button className="button button-primary" type="submit">Baseline Forecast 실행</button></form>{error ? <p className="text-critical">조회에 실패했습니다: {error}</p> : rows.length === 0 ? <p>실행 이력이 없습니다.</p> : <div className="data-table-wrap"><table className="data-table"><thead><tr><th>run_id</th><th>상태</th><th>실행 시간</th><th>모델 수</th><th>SKU 수</th><th>결과 행</th><th>data snapshot</th><th>stale</th><th>실행자</th></tr></thead><tbody>{rows.map((row) => <tr key={row.runId}><td><code>{row.runId}</code></td><td>{row.status}</td><td>{row.startedAt}</td><td>{row.nModels}</td><td>{row.nItems}</td><td>{row.nRows}</td><td>{row.dataSnapshotAt ?? '—'}</td><td><Badge status={row.isStale ? 'WARNING' : 'SAFE'} label={row.isStale ? 'STALE' : '최신'} /></td><td>{row.triggeredEmail ?? '—'}</td></tr>)}</tbody></table></div>}</Panel></div>; }
