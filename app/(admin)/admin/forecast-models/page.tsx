import PageHeader from '@/components/shell/page-header'; import Panel from '@/components/ui/panel'; import Badge from '@/components/ui/badge'; import { requireAdmin } from '@/lib/auth'; import { getForecastModels } from '@/lib/scm';
export const dynamic = 'force-dynamic';
export default async function ForecastModelsPage() { await requireAdmin(); const { rows, error } = await getForecastModels(); return <div className="page-content"><PageHeader title="Forecast Models" description="SQL Baseline 모델의 활성 상태와 버전을 확인합니다." /><Panel>{error ? <p className="text-critical">조회에 실패했습니다: {error}</p> : <div className="data-table-wrap"><table className="data-table"><thead><tr><th>모델명</th><th>family</th><th>engine</th><th>version</th><th>enabled</th><th>적용 수요 유형</th><th>parameters</th></tr></thead><tbody>{rows.map((row) => <tr key={row.modelId}><td>{row.modelName} ({row.modelId})</td><td>{row.family}</td><td>{row.engine}</td><td>{row.version}</td><td><Badge status={row.enabled ? 'SAFE' : 'CALCULATION_UNAVAILABLE'} label={row.enabled ? 'ON' : 'OFF'} /></td><td>{row.demandTypes.join(', ')}</td><td><code>{JSON.stringify(row.parameters)}</code></td></tr>)}</tbody></table></div>}</Panel></div>; }
