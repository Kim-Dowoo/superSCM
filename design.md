# superSCM 디자인 시스템

> 상태: 적용 기준 · 기준일: 2026-08-28  
> 대상: 한국후지필름BI 월간 발주계획 시스템의 모든 화면

## 1. 디자인 방향

superSCM은 **어두운 관제 콘솔 안에서 발주 판단에 필요한 신호를 빠르게 읽는 시스템**이다. 기본 테마는 다크 모드이며, 밝은 업무 화면과 별도의 라이트 테마를 제공하지 않는다.

- 배경은 깊은 네이비로 안정감과 집중도를 만든다.
- 시안은 사용자의 주의가 필요한 주 동작·선택 상태·핵심 데이터에만 사용한다.
- 초록·호박·빨강은 각각 정상·주의·위험의 업무 의미만 가진다.
- 장식보다 표, 지표, 상태, 다음 행동을 우선한다.
- 모든 문구는 한국어로 작성한다. 데이터 코드와 숫자는 원문을 유지한다.

첨부 시안의 `Nexus AI` 분위기(다크 네이비, 시안 발광, 촘촘한 운영 화면)를 따르되, 제품명은 기존 superSCM을 유지한다.

## 2. 구현 원칙

1. 새 CSS 프레임워크를 추가하지 않는다. 스타일은 `app/globals.css`만 사용한다.
2. 색상·간격·글자 크기를 개별 화면에서 직접 정하지 않는다. 이 문서의 CSS 변수와 공통 클래스를 사용한다.
3. 화면별 데이터 계산은 `lib/scm.ts` 또는 SQL 뷰에 둔다. 디자인 컴포넌트는 표시만 담당한다.
4. 기존 화면은 기능을 바꾸지 않고 공통 레이아웃과 컴포넌트 클래스부터 교체한다.
5. 새로운 클래스가 필요하면 화면 전용 이름 대신 역할 중심 이름을 `app/globals.css` 끝에 추가한다.

## 3. 디자인 토큰

`app/globals.css`의 `:root`에는 아래 값을 단일 기준으로 둔다. 이름은 의미를 나타내며, 색상 자체를 이름에 넣지 않는다.

| 역할 | CSS 변수 | 값 | 사용처 |
|---|---|---:|---|
| 앱 캔버스 | `--canvas` | `#031427` | `html`, `body` |
| 기본 표면 | `--surface` | `#0b1c30` | 카드, 사이드바 |
| 높음 표면 | `--surface-high` | `#1b2b3f` | 입력 필드, 내부 영역 |
| 가장 높음 표면 | `--surface-highest` | `#26364a` | 호버, 선택 보조 표면 |
| 기본 글자 | `--ink` | `#d3e4fe` | 제목, 본문 |
| 보조 글자 | `--muted` | `#c6c6cd` | 설명, 메타 정보 |
| 테두리 | `--line` | `#45464d` | 카드·표·입력 구분선 |
| 주 강조 | `--blue` | `#4cd7f6` | 기본 버튼, 선택, 링크, 포커스 |
| 정상 | `--green` | `#4edea3` | 완료, 연결됨, 안전 |
| 주의 | `--amber` | `#f2bd6b` | 검토 필요, 표본 부족 |
| 위험 | `--red` | `#ffb4ab` | 오류, 소진 위험, 파괴 동작 |

색상은 의미를 바꾸지 않는다. 예를 들어 `risk_status = CRITICAL`은 항상 빨강, `SAFE`는 항상 초록, `UNKNOWN`은 회색으로 표시한다. 시안은 위험 상태를 표현하지 않는다.

### 그림자와 발광

- 일반 카드는 그림자를 쓰지 않거나 매우 약하게 쓴다.
- 상호작용 가능한 카드와 주 버튼만 시안 그림자(`0 0 15px rgba(76, 215, 246, .15)`)를 호버 시 사용한다.
- 지속적인 애니메이션·과도한 유리 효과는 사용하지 않는다. 진행 상태처럼 시간 경과를 보여야 할 때만 애니메이션을 허용한다.

## 4. 타이포그래피와 숫자

기본 한글 글꼴은 `Noto Sans KR`이다. 영문 제품명·대제목은 `Geist`, 품목 코드·날짜·수치 등 비교가 중요한 데이터는 `JetBrains Mono`를 사용한다. 새 글꼴을 추가하기 전에는 시스템 폰트 대체를 허용한다.

| 역할 | 크기 / 줄높이 | 굵기 | 규칙 |
|---|---:|---:|---|
| 화면 제목 | 30px / 38px | 600~700 | 한 화면에 하나 |
| 섹션 제목 | 18px / 26px | 600 | 아이콘은 필요할 때만 |
| 카드 제목 | 14px / 20px | 600~700 | 짧고 업무 중심으로 |
| 본문 | 14px / 20px | 400 | 설명과 안내 |
| 표·보조 본문 | 12px / 18px | 400~500 | 데이터 밀도 유지 |
| 레이블 | 11px / 16px | 700 | 대문자 영문 대신 한국어 사용 가능 |
| 핵심 지표 | 28~32px / 36px | 700~800 | 숫자는 `tabular-nums` 사용 |

- 금액, 수량, 날짜, 품목 코드는 고정폭 숫자를 적용하고 오른쪽 정렬한다.
- 날짜는 `YYYY-MM-DD`, 일수는 `18일`, 수량은 `1,240개`처럼 단위를 함께 표시한다.
- 계산 불가 값은 `—`로 표시하고, 필요하면 원인 배지(`사용 이력 없음`)를 함께 둔다. `0`이나 임의의 큰 수로 대체하지 않는다.

## 5. 공통 레이아웃

데스크톱은 고정 사이드바와 상단바를 사용한다. 콘텐츠 최대 폭은 1,440px이며, 화면이 넓어져도 정보가 지나치게 퍼지지 않게 한다.

| 영역 | 클래스 | 규격 |
|---|---|---|
| 앱 전체 | `app-shell` | 최소 높이 100vh, 다크 캔버스 |
| 사이드바 | `sidebar` | 폭 240px, 낮은 표면, 우측 구분선 |
| 상단바 | `topbar` | 높이 64px, 반투명 배경, 하단 구분선 |
| 본문 | `content` | 최대 폭 1,440px, 데스크톱 24px 여백 |
| 분석 본문 | `analysis-page` | 동일한 콘텐츠 폭과 여백을 상속 |

### 탐색

- 메뉴는 아이콘과 한국어 레이블을 함께 쓴다. 아이콘만으로 기능을 전달하지 않는다.
- 현재 메뉴는 왼쪽 3~4px 시안 막대, 시안 글자, 약한 시안 배경으로 표현한다.
- 일반 메뉴의 호버는 `--surface-highest` 배경만 사용한다.
- 단계형 업무 흐름은 현재 단계·완료 단계·미완료 단계를 각각 시안·초록·보조 글자로 구분한다.

## 6. 컴포넌트 규칙

### 카드와 KPI

- 기본 카드: `card`. 낮은 표면, 1px 테두리, 8px 모서리, 16~24px 내부 여백.
- KPI 카드: `card metric`. 레이블 → 값 → 해석 순서로 배치한다.
- KPI 값은 수치 하나만 강조하고, 의미 있는 보조 문구를 반드시 둔다. 예: `위험 품목 4` / `30일 이내 소진 예상`.
- 클릭 가능한 KPI에는 키보드 포커스, 호버 윤곽, 이동 가능한 상세 화면 또는 필터 적용 결과가 있어야 한다.

### 버튼

| 종류 | 클래스 | 용도 |
|---|---|---|
| 주요 동작 | `button primary` | 저장, 확정, 분석 실행처럼 화면당 하나의 핵심 행동 |
| 보조 동작 | `button` | 취소, 내보내기, 상세 보기 |
| 약한 강조 | `button ghost` | 필터 초기화, 보조 이동 |
| 위험 동작 | `button danger` | 삭제 등 되돌리기 어려운 행동. 확인 절차 필요 |

- 버튼 높이는 최소 36px, 클릭 영역은 최소 44×44px를 권장한다.
- 같은 문맥에서 주요 버튼은 한 개만 둔다.
- 아이콘만 쓰는 버튼은 `aria-label`을 제공하고 툴팁을 둔다.

### 상태 배지와 메시지

`tag`는 짧은 상태에만 사용하고 문장 전체를 넣지 않는다.

| 상태 | 클래스 | 예시 |
|---|---|---|
| 정상·완료·안전 | `tag green` | 안전, 연결됨, 완료 |
| 검토·보류·표본 부족 | `tag amber` | 주의, 확인 필요 |
| 위험·오류·소진 임박 | `tag red` | 위험, 실패 |
| 미정·계산 불가 | `tag gray` | 미정, 사용 이력 없음 |
| 선택·정보 | `tag blue` | 분석 기준, 선택됨 |

- 오류와 빈 결과를 같은 화면으로 처리하지 않는다. 오류에는 원인과 재시도·확인 방법을, 빈 결과에는 조건 변경이나 데이터 준비 안내를 제공한다.
- `callout`은 정보, 주의, 위험 상황의 다음 행동을 한 문장으로 설명할 때 쓴다.

### 표와 데이터 밀도

- 표 컨테이너는 `table-wrap` 또는 `analysis-table-wrap`을 사용하며 작은 화면에서는 가로 스크롤을 허용한다.
- 헤더는 높은 표면, 11~12px, 굵게 표시하고 스크롤해도 의미가 흐려지지 않게 짧은 명칭을 쓴다.
- 수치 열은 오른쪽 정렬, 식별자·이름은 왼쪽 정렬한다.
- 행 높이는 기본 44px 내외, 행 호버는 한 단계 높은 표면으로 제한한다.
- 색만으로 위험을 구분하지 않는다. `위험` 배지, `+3일`처럼 텍스트·기호도 같이 제공한다.
- 테이블 안에서 인라인 편집을 할 때 저장 여부와 검증 오류를 해당 셀 가까이에 표시한다.

### 입력과 필터

- 입력·선택 상자는 `--surface-high` 배경과 `--line` 테두리를 사용한다.
- 포커스는 시안 테두리와 3px 반투명 시안 링으로 명확하게 보인다.
- 레이블은 플레이스홀더를 대신하지 않는다.
- 필터는 화면 상단 또는 표 상단 한곳에 모으고, 적용된 조건과 초기화 동작을 함께 제공한다.

## 7. 화면 구성 기준

모든 분석 화면은 아래 순서를 기본으로 한다.

1. 화면 제목과 한 문장 목적
2. 범위·기간·새로고침 등 화면 동작
3. 핵심 KPI 3~4개
4. 판단을 위한 상세 표 또는 분석 영역
5. 경고·제약·데이터 기준 설명

`app/analysis/leadtime/page.tsx`의 `AnalysisFrame`, `DataTable`, `grid`, `card`, `metric` 구조를 기본 뼈대로 사용한다. 새 화면도 이 구조에서 시작하고, 예외가 필요한 경우에만 새로운 공통 클래스를 만든다.

## 8. 반응형 규칙

- 1,050px 이하: 사이드바 폭을 줄이고 KPI 4열을 2열로 바꾼다.
- 760px 이하: 사이드바는 상단 탐색으로 전환하고, KPI·카드 그리드는 1열로 쌓는다.
- 모바일에서 표는 열을 억지로 숨기지 않는다. 우선순위가 낮은 열만 별도 상세 화면으로 보내거나 가로 스크롤을 사용한다.
- 주요 버튼은 좁은 화면에서 제목 아래로 내려가며, 핵심 동작의 너비는 충분히 확보한다.

## 9. 접근성과 품질 기준

- 일반 본문은 배경 대비 4.5:1 이상을 유지한다.
- 마우스 호버와 동일한 피드백을 `:focus-visible`로 제공한다.
- 상태 전달은 색상·아이콘·텍스트 중 최소 두 가지를 함께 사용한다.
- `prefers-reduced-motion` 환경에서는 전환·발광·진행 애니메이션을 최소화한다.
- 키보드만으로 메뉴, 버튼, 입력, 표 내부의 상호작용을 완료할 수 있어야 한다.

## 10. 적용 순서와 검수

1. `app/globals.css`의 기존 라이트 토큰을 본 문서의 다크 토큰으로 교체한다.
2. `app-shell`, `sidebar`, `topbar`, `content`를 먼저 바꿔 전체 골격을 통일한다.
3. `card`, `button`, `tag`, 표, 입력 필드 순으로 공통 요소를 변경한다.
4. 리드타임·소진위험 등 분석 화면에서 예외 스타일을 제거하고 공통 클래스로 수렴한다.
5. 데스크톱과 760px 이하 화면, 키보드 포커스, 오류·빈 결과 상태를 확인한다.
6. 변경 후 `npm run build`를 실행한다.

새 화면·수정 화면을 검수할 때 다음을 확인한다.

- [ ] 다크 캔버스와 표면 단계가 토큰만으로 구성되었는가
- [ ] 화면당 주요 버튼이 하나인가
- [ ] 위험·주의·정상 상태가 같은 의미와 같은 배지를 쓰는가
- [ ] 수치가 오른쪽 정렬되고 단위·계산 불가 사유가 명확한가
- [ ] 오류와 빈 결과를 구분하는가
- [ ] 760px 이하에서 읽기와 조작이 가능한가
- [ ] 새 CSS 프레임워크 또는 화면별 임의 색상을 추가하지 않았는가

## 11. 금지 사항

- Tailwind, CSS Modules, styled-components 등 새 스타일 체계 도입
- 화면 컴포넌트 안의 계산식, 상태별 임의 색상, 인라인 스타일 남발
- 의미 없는 그라데이션·발광·자동 애니메이션
- 빨강·초록만으로 상태를 구분하는 표현
- 계산할 수 없는 값을 `0`, `999`, 빈 문자열로 대신 표시하는 처리
- 데이터가 많다는 이유로 모바일에서 핵심 판단 정보를 숨기는 처리
